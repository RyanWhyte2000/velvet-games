apiVersion: v1
kind: Service
metadata:
  name: {{ include "velvet-reels.fullname" . }}
  labels:
    {{- include "velvet-reels.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "velvet-reels.selectorLabels" . | nindent 4 }}
